{
  "name": "dnd-swa",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "generate-list": "node scripts/generate_entity_list.js",
    "verify-session-sync": "node scripts/verify_session_sync.js",
    "sync-check": "npm run generate-list && npm run verify-session-sync -- --session latest && npm run build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "marked": "^15.0.11",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.2.0"
  }
}
